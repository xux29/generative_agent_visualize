# 健康机制改动前后对比说明

本文对比 `dtx` 分支相对改动前（以 `health` 分支机制散落状态为「前」）的主要差异，便于评审与对接老师关注点。

---

## 1. 一句话对比

| | 改动前 | 改动后 |
|--|--------|--------|
| 机制代码 | 散落在 `agent_health` / `scorer*` / `strategy` 等 | 统一在 `modules/health_mechanisms/` |
| 参数 | 大量硬编码；后已有 JSON，但易被理解成「机制=JSON」 | JSON 明确为 **Config 层**；代码为 **Mechanism 层** |
| AI 能力边界 | 仿真 LLM 只做意图/策略决策，不能改机制 | 增加 **改参数 + 改机制代码** 双通道（白名单 + 批准） |
| 版本 | 主要有参数 `VersionStore` | 增加 **Model Version**（config + code 快照） |
| 可视化 | 原有回放 / 结果图 | **不新增**机制调参面板（已回退）；继续用原有可视化 |
| 机制清单 | 散落、无统一注册 | `registry.json` 登记 **17** 项（全量见架构文档 §5） |

---

## 2. 已提取机制全量清单

与 [`registry.json`](../generative_agents/data/mechanism/registry.json) 一致：

| ID | 标题 | 配置路径 / 数据 |
|----|------|-----------------|
| `health_score` | 健康分（非线性主路径） | `simulation.health` |
| `health_score_linear` | 健康分（线性兼容） | `simulation.health` |
| `mood` | 情绪分 | `simulation.mood` |
| `satisfaction` | 满意度 | `simulation.satisfaction` |
| `relapse` | 复发（被管者行为） | `simulation.relapse` |
| `habit` | 习惯 streak / 倾向 | `simulation.habit` |
| `compliance` | 劝说服从 / 抵抗 | `simulation.compliance` |
| `intention` | 意图生成 | `simulation.subject` |
| `violation` | 违规判定 / 睡眠意图归一 / 日终未阻止计数 | 尚未外置 JSON |
| `env_block` | 环境阻止（锁厨/移食/收手机） | 尚未外置 JSON |
| `turnaround` | 折返独白与当日干预降级 | 尚未外置 JSON（prompt: `health_generate_turnaround`） |
| `intervention_execute` | 干预执行 | `management.intervention` |
| `management` | 长期管理策略 | `management` |
| `asymmetric_game` | 信息不对称博弈 | 尚未外置 JSON |
| `scenario` | 场景语义配置 | `data/semantic_mapping.json` |
| `behavior_scores` | 行为分项表 | `simulation.behavior_scores` |
| `composite` | 综合评估 | `simulation.composite` |

实现类路径、prompts 绑定等完整字段见  
[健康机制双层架构与AI编辑.md §5](./健康机制双层架构与AI编辑.md)。

---

## 3. 目录结构对比

### 改动前（机制相关，示意）

```text
modules/
├── agent_health.py          # Mixin + 复发/意图/干预
├── scorer.py / scorer_nonlinear.py
├── strategy.py
├── asymmetric_game.py
├── intention.py
├── scenario_config.py
└── prompt/scratch.py        # 含 prompt_health_*
data/prompts/health_*.txt
data/mechanism/              # 参数 JSON（已有）
```

### 改动后

```text
modules/health_mechanisms/   # 机制代码本体（统一入口）
├── agent_mixin.py / scoring/ / management / rules/ / ...
├── prompts.py / registry.py
├── model_version.py / ai_edit.py / sandbox.py
modules/*                    # 旧路径 → thin re-export（兼容）
data/mechanism/
├── active.json / defaults / registry.json
├── prompts/health_*.txt     # prompt 主路径
├── versions/                # 参数版本
├── models/ · proposals/     # Model/草稿（本地，不入库）
tools/mechanism_cli.py       # 参数 CLI（原有）
tools/model_cli.py           # Model / AI 编辑 CLI（新增）
```

`rules/` 含违规判定、环境阻止、折返（从 `start_health_simulation` 抽出的纯机制函数）。

斯坦福小镇基座（`start.py`、`think`、memory、maze 寻路等）**位置不变**。

---

## 4. 能力对比（针对老师担心的点）

| 能力 | 改动前 | 改动后 |
|------|--------|--------|
| 专家调数值 | 改 JSON / CLI | 同左（`mechanism_cli` / `active.json`） |
| AI 改参数 | 无专用通道 | `model_cli edit-config`（先写 proposal） |
| AI 改/增机制逻辑 | 无；只能改决策文案 | `model_cli edit-code`（限 `health_mechanisms/` + health prompts） |
| 是否被 JSON schema 限死 | 若把 JSON 当机制本体则会 | **不会**：缺机制时改 Python，再可选暴露新参数到 JSON |
| 改坏如何回退 | 参数 rollback | 参数 rollback + Model `approve`/`rollback`（可含代码） |
| 默认是否动线上 | — | **默认不动** live；需 `promote` → `approve` |

---

## 5. 运行与入口对比

| 用途 | 改动前 | 改动后 |
|------|--------|--------|
| 跑健康仿真 | `start_health_simulation.py` | 相同；机制从 `health_mechanisms` import |
| 参数版本 | `tools/mechanism_cli.py` | 相同 |
| AI/专家机制治理 | 无 | `tools/model_cli.py` + 两个 Cursor Skill |
| 地图回放 | `replay_health.py` (:5001) | 相同 |
| 结果图表 | `visualize_health.py` | 相同（**无** `/mechanism` 面板） |

---

## 6. Cursor Skills（对话约束）

| Skill | 改什么 | 不能改什么 |
|-------|--------|------------|
| `edit-mechanism-config` | `data/mechanism` 参数 JSON / proposal config | 任意机制 Python、基座、前端 |
| `edit-health-mechanisms` | `health_mechanisms/**`、`prompts/health_*.txt`、`registry.json` | `agent.think`、日循环编排、maze、旧 re-export 壳、可视化等 |

详细流程与禁止列表见架构文档 §4，以及：

- `.cursor/skills/edit-mechanism-config/SKILL.md`
- `.cursor/skills/edit-health-mechanisms/SKILL.md`

---

## 7. 明确「未做 / 已回退」

- 未把 JSON 定义为模拟机制本体。
- 未让仿真内 LLM 直接改仓库代码（治理改动走 Cursor Skill + CLI，且有白名单）。
- **已删除** Phase 4 轻量机制可视化面板相关文件与路由，避免与现有可视化重复、增加维护面。

---

## 8. 相关文档

- [健康机制双层架构与AI编辑.md](./健康机制双层架构与AI编辑.md) — 架构、Skill、用法
- [机制参数JSON使用指南.md](./机制参数JSON使用指南.md) — 参数 JSON / CLI
