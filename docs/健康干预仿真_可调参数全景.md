# 健康干预仿真 · 可调参数

> **更新提示**：多数「硬编码」项已进入 `data/mechanism/*.json`。  
> **调参因果（↑/↓ 对模拟的影响）**请以 [`mechanism/机制参数升降影响手册.md`](./mechanism/机制参数升降影响手册.md) 为准并持续补充；本文保留作早期盘点目录。

## 一、模拟机制

### 1.基础主体属性

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `self_discipline` | medium（场景常 low/very_low） | 自律等级；驱动健康惩罚、复发、劝说接受率 | CLI --discipline / semantic_mapping / agent.json | 已可调 | 应该有低中高三档 |
| `addiction_level` | moderate / severe（场景） | 成瘾强度 → 复发与抵抗修正 | semantic_mapping.json → target_profile | 已可调 |  |
| `resistance_to_persuasion` | 0.2–0.8 | 劝说抵抗基线 | semantic_mapping.json | 已可调 |  |
| `initial_health` | 75（可选 60/75/90） | 健康分起点 | CLI --initial-health | 已可调 |  |
| `habit_formation_speed` | slow / very_slow / normal | 习惯形成速度（主要进 prompt） | semantic_mapping.json | 已可调 |  |

### 2.健康分演算

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `WARNING_LINE` | 30 | 健康崩溃警戒线；成功判定阈值 | scorer.py / scorer_nonlinear.py（config 字段不同步） | 硬编码 |  |
| `natural_recovery` | high3 / med2 / low1.5 | 无违规时每日自然恢复分 | CumulativeHealthScorer.DISCIPLINE_PARAMS | 硬编码 | 删除 |
| `plateau_duration` | 14 / 7 / 3 天 | 平台期长度（按自律） | DISCIPLINE_PARAMS | 硬编码 |  |
| `decline_rate` | 0.5 / 1.0 / 2.0 | 平台后自然下滑速度（分/天） | DISCIPLINE_PARAMS | 硬编码 |  |
| `violation_penalty` | 10 / 12 / 15 | 未阻止违规的基础扣分 | DISCIPLINE_PARAMS | 硬编码 |  |
| `compliance_bonus` | 2 / 1.5 / 1 | 遵从奖励（上限≤初始分） | DISCIPLINE_PARAMS | 硬编码 |  |
| `mood_sensitivity` | 0.7 / 1.0 / 1.5 | 干预对情绪/心情的放大系数 | DISCIPLINE_PARAMS | 硬编码 |  |
| `blocked_violation_factor` | ×0.3 | 意图被拦仍小幅扣分 | scorer.py | 硬编码 | 删除 |
| `MAX_EFFECTIVE_VIOLATIONS` | 1 | 单日最多计 1 次违规惩罚 | scorer.py | 硬编码 | 删除 |
| `HEALTH_ZONES / floor_protection` | 区间敏感度 + 低分保护 0.25–1.0 | 非线性计分区间与低分保护（--scoring nonlinear） | scorer_nonlinear.py | 硬编码 |  |
| `habit_bonus / max_penalty` | min(0.3, days×0.02) / -10 | 连续好天恢复加成；单日扣分上限 | scorer_nonlinear.py | 硬编码 |  |
| `IMPROVEMENT_THRESHOLD` | 14 | 连续无违规天数 → 可提升自律等级 | scorer_nonlinear.py | 硬编码 |  |

### 3.复发概率

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `base_prob` | 0.15 | 放松后「又干坏事」基础概率 | agent_health.py | 硬编码 |  |
| `discipline_mod` | very_low 2.0 … very_high 0.2 | 自律越低越易复发 | agent_health.py | 硬编码 |  |
| `addiction_mod` | none 0.5 … severe 2.0 | 成瘾越重越易复发 | agent_health.py | 硬编码 |  |
| `time_mod` | (days_relaxed/3)×0.15 | 放松越久复发越高（约每 3 天 +15%） | agent_health.py | 硬编码 |  |
| `habit_mod` | max(0.3, 1−streak×0.03) | 习惯连续天数抑制复发 | agent_health.py | 硬编码 |  |
| `tendency_mod / Δtendency` | 1+tendency×0.5；好−0.05 / 差+0.2 / 复发+0.3 | 复发倾向演化与修正 | agent_health.py | 硬编码 |  |

### 4.心情/情绪生成

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `generate_daily_mood 阶段分布` | D1–7 stressed；D8–21 normal；D22+ 含 depressed | 被管者天然情绪标签按阶段采样 | agent_health.py | 硬编码 |  |
| `mood BASE_SCORE` | 6.0 | 心情分基线 | Scorer.calculate_mood_score_with_discipline | 硬编码 |  |
| `频率/强度惩罚` | −0.5×count×sens（下限−3）；L1/2/3: −0.3/−0.6/−1.0 | 干预越多越强 → 心情越差 | scorer.py | 硬编码 |  |
| `habit_bonus（心情）` | streak≥14:+1.5；≥7:+1；≥3:+0.5 | 习惯稳固提升心情 | scorer.py | 硬编码 |  |
| `neglect / plateau 惩罚` | health<50且0干预:−1.0×sens；平台+L≥2:−1.5×sens | 该管不管焦虑；好了还严管不满 | calculate_mood_score_with_discipline | 硬编码 |  |

### 5.行为遵从与抵抗

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `无干预自觉率基线` | very_low0.2 … very_high0.9 | 按自律决定「没人盯时是否守规矩」 | agent_health.get_behavior_tendency | 硬编码 |  |
| `effective_resistance` | resistance + 成瘾修正 + 自律修正 | 劝说/移除时的实际抵抗；L3 反抗≈×0.7 | agent_health（干预响应） | 硬编码 |  |

### 6.行为维度扣分表

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `FOOD_TYPE / AMOUNT_SCORES` | 高糖高脂 diabetes−4 / weight−5；过量−4/−5 | 进食种类与分量对三场景的分项影响 | Scorer 常量 | 硬编码 |  |
| `PHONE_END_TIME / ACTIVITY` | 22点前 sleep+5；1点后−5；游戏 sleep−5 | 手机结束时间与活动类型评分 | Scorer 常量 | 硬编码 |  |
| `餐睡间隔 / 连续时长公式` | (t−180)×0.04\|0.02\|0.03；(30−t)×0.15 | 连续量行为的线性公式系数 | Scorer.calc_* | 硬编码 |  |

### 7.习惯连续&自律提升

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `habit_streak 更新规则` | 好+1；差归零；倾向 −0.05 / +0.2 | 连续表现追踪；联动复发倾向 | agent_health.update_habit_streak | 硬编码 |  |
| `habit_streak_modifier 阈值` | 5/10/14/21 天阶梯 | streak × 干预强度 → 满意度修正 | Scorer.calc_habit_streak_modifier | 硬编码 |  |
| `内化阶段阈值` | ≥0.8 autonomous；≥0.6 internalized；≥0.3 compliant | 顺从→内化→自主判定 | HabitConsolidation | 硬编码 |  |

### 8.满意度计算

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `satisfaction BASE_SCORE` | 6.0 | 满意度基线 | Scorer.calculate_satisfaction_score | 硬编码 |  |
| `INTERVENTION_LEVEL_WEIGHTS` | 0 / −0.3 / −0.6 / −1.0 | 干预等级强度惩罚 | scorer.py | 硬编码 |  |
| `频率惩罚` | −0.5×count，上限 −2.0 | 干预次数越多满意度越低 | scorer.py | 硬编码 |  |
| `INTERVENTION_REASONABILITY` | 合理+0.5 / 预防+0.3 / 多余−0.5 | 时机与合理性修正 | scorer.py | 硬编码 |  |
| `DISCIPLINE_PHASE_MODIFIERS` | ≤7调整 / ≤21形成 / 其后疲劳 | 阶段敏感度与基础偏移 | scorer.py | 硬编码 |  |
| `overintervention_penalty` | streak≥7 且 health≥7 且 level≥2 | 健康已好仍严管 → 过度干预惩罚 | calc_overintervention_penalty | 硬编码 |  |
| `autonomy_modifier` | 上限 +0.5 | 提供替代选项 → 满意度提升 | calc_autonomy_modifier | 硬编码 |  |

### 9.综合评估

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `COMPOSITE_WEIGHTS` | 健康0.5 / 满意度0.3 / 效果0.2 | 综合分权重 | scorer.py | 硬编码 |  |
| `EFFECTIVENESS_THRESHOLDS` | excellent2 / good1 / neutral0 / poor−1 | 效果分档阈值 | scorer.py | 硬编码 |  |
| `综合评级阈值` | A≥8 / B≥6.5 … | 综合分字母等级 | calculate_composite_score | 硬编码 |  |

## 二、管理机制

### 1.干预等级体系

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `Strategy Level 0–3` | observe / persuade / remove / lock | 干预强度阶梯 | strategy.Strategy | 硬编码 |  |
| `escalation_threshold` | 2–3（场景） | 失败几次升级 | semantic_mapping manager_profile | 已可调 |  |
| `supervision_style / authority_level` | supportive/gentle/…；low/med/high | 监督风格与权威感（影响 LLM 话术） | semantic_mapping.json | 已可调 |  |
| `monitoring_hours / phone_allowed_hours` | 如 19:00–02:00 / 06:00–21:00 | 监控与允许时段 | semantic_mapping.json | 已可调 |  |

### 2.潮汐式松紧

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `health_threshold_bad/good` | 糖尿病5/7；减肥4/6；睡眠4/6 | 判定「不好/好」的日健康阈值 | StrategyManager.SCENARIO_SEVERITY | 硬编码 |  |
| `consecutive_good/bad_days` | 放松 3/5/7；升级 1/2/3 | 连好才放松、连坏就升档 | SCENARIO_SEVERITY | 硬编码 |  |
| `relapse_probability_base（策略侧）` | 0.4 / 0.3 / 0.25 | 潮汐策略感知的复发基线（与被管者 0.15 公式并列） | SCENARIO_SEVERITY | 硬编码 |  |
| `severity_weight` | 糖尿病1.5 / 减肥1.0 / 睡眠0.7 | 复发后升档幅度：≥1.5→L3；≥1.0→+2；else +1 | SCENARIO_SEVERITY / on_relapse | 硬编码 |  |
| `trust_level 初值与增减` | 0.5；好+0.1 / 差−0.15 / 过度−0.2；放松需≥0.6 | 短期信任驱动能否降档 | StrategyManager._update_trust | 硬编码 |  |

### 3.长期关系阶段

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `HONEYMOON` | mod 0.8；trust×1.5；min_days3；conflict≥2 退出 | 建立规则期，干预偏温和 | LongTermStrategyManager.PHASE_CONFIG | 硬编码 |  |
| `ADJUSTMENT` | mod 1.0；min_days7；compliance≥0.6；连好≥5 | 习惯形成与冲突处理 | PHASE_CONFIG | 硬编码 |  |
| `FATIGUE` | mod 0.9；min_days14；需内化+高自主 | 防倦怠期 | PHASE_CONFIG | 硬编码 |  |
| `STABLE` | mod 0.6；trust×0.8；仅复发退出 | 维持自主，偶尔提醒 | PHASE_CONFIG | 硬编码 |  |
| `RELAPSE` | mod 1.3；trust×0.5；连好≥5 退出 | 复发重建期，干预加严 | PHASE_CONFIG | 硬编码 |  |

### 4.信任资本

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `initial_capital / max` | 50 / 100 | 信任账户起点与上限 | TrustCapital | 硬编码 |  |
| `autonomy 档位阈值` | ≥80 high；≥50 med；≥20 low；else minimal | 资本 → 自主权等级 | TrustCapital.get_autonomy_level | 硬编码 |  |
| `日赚取/损失` | 好+2；主动+3；无干预+2；差−5；稳定期差−8；复发−10 | 信任账户日结算 | _update_trust_capital | 硬编码 |  |

### 5.习惯巩固追踪

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `内化评分权重` | 无干预0.4 + 主动0.3 + 情绪0.3 | 区分暂时顺从 vs 真正内化 | HabitConsolidation | 硬编码 |  |

### 6.管理者学习曲线

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `understanding / efficiency 增量` | 成功 0.02×(1−u)；失败 +0.005；周期 +0.05 | 经验增长 → 干预更高效/更早 | ManagerLearning | 硬编码 |  |
| `early_intervention_threshold` | 50 + understanding×20 | 越了解越早介入 | ManagerLearning | 硬编码 |  |
| `效率降级规则` | eff≥1.3 降1级；≥1.1 且 base≥2 降1 | 熟练后倾向更温和干预 | get_recommended_intervention_level | 硬编码 |  |

### 7.情绪感知&过度干预保护

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `emotion_warning / critical` | 3.0 / 2.5 | 情绪警告与强制降级 | OVER_INTERVENTION_CONFIG | 硬编码 |  |
| `high/medium_trust_threshold` | 80 / 50 | 高信任启用宽松模式 | OVER_INTERVENTION_CONFIG | 硬编码 |  |
| `max_consecutive_high_interventions` | 5 | 连续高强度干预保护 | OVER_INTERVENTION_CONFIG | 硬编码 |  |
| `health_safe_threshold` | 6 | 健康分高于此值时可放松干预 | OVER_INTERVENTION_CONFIG | 硬编码 |  |
| `强制放松天数` | 连续低情绪≥3天 → 放松3天 | 防管太猛反弹 | _check_over_intervention_protection | 硬编码 |  |

### 8.周期性反思与调整

| 参数 | 默认值 | 作用 | 来源 | 可调性 | 是否控制变量 |
|------|--------|------|------|--------|--------------|
| `周/月反思周期` | 每7天 / 每30天 | 触发周回顾与月总结 | _check_periodic_reflection | 硬编码 |  |
| `周干预过高阈值` | >20 次 | 识别「干预过多」模式 | _generate_weekly_reflection | 硬编码 |  |