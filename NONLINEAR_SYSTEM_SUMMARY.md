# 非线性健康评分系统

## 一、系统概述

非线性健康评分系统（`NonlinearHealthScorer`）模拟真实人类健康指标的非线性动态，相比传统线性系统有以下核心改进：

1. **阈值效应**：不同健康区间有不同的敏感度
2. **边际递减**：恢复速度随距离目标的距离而变化
3. **累积效应**：连续违规产生复合影响
4. **随机波动**：模拟日常生理/心理变异
5. **个体差异**：同等级内参数有分布范围

---

## 二、健康区间（HealthZone）

系统将健康分划分为5个区间：

| 区间        | 分数范围   | 敏感度  | 描述          |
| --------- | ------ | ---- | ----------- |
| EMERGENCY | 0-29   | 1.2  | 紧急状态        |
| DANGER    | 30-49  | 1.1  | 危险状态，接近生理极限 |
| WARNING   | 50-69  | 1.05 | 预警状态，缓冲减少   |
| NORMAL    | 70-89  | 1.0  | 正常基线状态      |
| SAFE      | 90-100 | 0.95 | 安全状态，生理缓冲充足 |

**敏感度**：违规时惩罚的放大系数，值越大惩罚越重

---

## 三、自律等级参数（SelfDisciplineLevel）

### 3.1 自律等级定义

| 等级        | 违规扣分范围  | 累积系数  | 波动σ  | 韧性  | 平台期 |
| --------- | ------- | ----- | ---- | --- | --- |
| HIGH（高）   | 0.5-1.5 | 0.005 | 0.15 | 2.5 | 14天 |
| MEDIUM（中） | 0.5-1.5 | 0.005 | 0.3  | 1.4 | 7天  |
| LOW（低）    | 0.5-1.5 | 0.005 | 0.4  | 1.2 | 3天  |

---

## 四、评分公式

### 4.1 每日分数变化

```
change = 违规惩罚  + 遵从奖励 + 加速下滑 + 每日波动
```

### 4.2 违规惩罚公式

```
penalty = -base_penalty × cumulative_mult × zone_sensitivity × resilience_mult × violation_count

其中：
- base_penalty ~ Uniform(penalty_min, penalty_max)  # 个体差异随机值
- cumulative_mult = 1.0 + consecutive_violations × cumulative_factor  # 累积效应
- zone_sensitivity = 健康区间敏感度（见区间表）
- resilience_mult = 1.0 ± N(0, 0.1) × resilience  # 抗压能力波动
- violation_count = 实际违规次数

注意：已移除底线保护机制（floor_protection），低分时惩罚全额执行
```

### 4.4 加速下滑公式

连续违规≥3天时触发：

```
acceleration = -0.12 × (consecutive_bad_days - 2)^1.3 × zone_sensitivity

限制：acceleration ∈ [-2.5, 0]
注意：当预估分数 < 20时，禁用加速下滑（更温和处理临界状态）
```

### 4.5 每日波动

```
noise ~ N(0, noise_sigma)

限制：|noise| ≤ 1.2  # 已收窄波动范围
特殊限制（分数 < 20）：noise ∈ [-0.8, 0.8]  # 低分区更稳定
```

---

## 五、管理介入机制（替代原软地板/固定等级）

### 5.1 管理强度分级（动态，非固定阈值）

管理强度根据低分持续时间和分数变化速率动态调整：

| 状态                | 干预成功率    | 遵从奖励  |
| ----------------- | -------- | ----- |
| 正常（分数 ≥ 30，无快速下降） | 60%      | +1.5分 |
| 低分1-2天            | 65%      | +2.0分 |
| 低分3-6天            | 75%      | +2.5分 |
| 低分7-13天           | 85%      | +3.0分 |
| 低分 ≥ 14天          | **100%** | +3.5分 |
| **分数快速下降**        | 80%      | +3.0分 |

**分数快速下降定义**：5天内分数下降 ≥ 15分，触发"快速下降"状态，强化管理介入。

**核心变化**：

- 不再固定以分数阈值触发L1/L2/L3，而是"低分持续时间"决定管理强度
- 任何分数区间的agent，只要连续低分够久，都会触发强力干预
- 达到14天临界状态时，**任何分数**的干预都会成功
- 分数快速下降时，跳级加强管理干预

### 5.2 干预行为描述

管理agent在模拟期间的行为特征：

- **违规意图频率**：用户几乎60-85天内每天都有1-5次违规意图
- **每日干预**：管理者几乎每天都会采取一定的干预措施
- **调整依据**：根据agent的健康分、满意度、当前时期调整干预策略
- **低分强化**：当分数 < 30时，加强干预次数和强度（提高干预成功率和奖励）

### 5.4 干预策略调整

管理者会根据以下因素动态调整干预策略：

**健康分维度**：

- 分数 ≥ 60：轻度干预（L1），以提醒和教育为主
- 分数 40-59：中度干预（L2），增加检查频率和警告
- 分数 30-39：重度干预（L3预备），强化说服
- 分数 < 30：L3强制干预，干预必成功

**快速下降应对**：

- 检测到5天内下降≥15分时，立即提升管理强度至"低分3-6天"级别
- 追踪分数变化速率，速率越大干预越频繁

### 5.5 分数地板（已调整）

```
new_score = max(20, min(100, new_score))  # 上限100，下限20软地板
```

**变化**：新增SOFT_FLOOR = 20，当分数<20时限制下降，但允许在20附近波动。

---

## 六、自律提升机制

### 6.1 提升规则

- LOW → MEDIUM：连续10天无违规（原14天，已缩短）
- MEDIUM → HIGH：连续10天无违规（原14天，已缩短）

---

## 七、特殊机制

### 7.1 平台期（Tide Plateau）

连续表现良好达到一定天数后进入平台期：

- HIGH：14天
- MEDIUM：7天
- LOW：3天

平台期期间分数相对稳定。

### 7.2 遵从奖励

干预成功时额外奖励：

- 低分1-2天：+2.0分
- 低分3-6天：+2.5分
- 低分7-13天：+3.0分
- 低分≥14天：+3.5分（最高奖励）

**仅在低于初始分时生效**

### 7.3 被阻止的违规

干预成功阻止的违规不扣分（penalty = 0）。

### 7.4 每日最大变动限制

```
|change| ≤ 10（每天最多扣10分，最多加10分）
```

---

## 

### 8.1 低自律初始60分，每天违规

| 天数  | 分数   | 变化   | 状态                |
| --- | ---- | ---- | ----------------- |
| 1   | 57.5 | -2.5 | 违规                |
| 7   | 42.3 | -2.4 | 违规                |
| 14  | 35.1 | -1.8 | 持续低分              |
| 30  | 28.5 | -1.2 | EMERGENCY（管理介入加强） |
| 45  | 15.2 | +0.5 | EMERGENCY（L3干预成功） |
| 60  | 18.7 | +3.5 | 恢复中               |
| 90  | 12.3 | -1.5 | 再次低分（管理持续加强）      |

**注意**：分数可以降到个位数，管理强度随低分持续时间逐步加强，14天+时干预必成功

### 8.2 低自律初始60分，周末偶尔自律

| 天数  | 分数   | 变化   | 状态          |
| --- | ---- | ---- | ----------- |
| 1   | 57.5 | -2.5 | 违规          |
| 7   | 47.2 | +1.8 | 无违规         |
| 14  | 50.5 | +3.3 | 无违规         |
| 30  | 38.4 | -2.1 | 违规（管理L2介入）  |
| 45  | 25.6 | -2.8 | 持续低分        |
| 60  | 30.2 | +4.6 | 无违规（L3干预成功） |
| 90  | 22.1 | +2.4 | 低分持续        |

**注意**：偶尔自律可以延缓分数下降，但一旦进入持续低分状态，管理强度会逐步加强

---

## 九、关键参数汇总

### 9.1 健康区间参数

```python
HEALTH_ZONES = {
    EMERGENCY: {"range": (0, 29), "sensitivity": 1.2, "recovery_mult": 0.8},
    DANGER: {"range": (30, 49), "sensitivity": 1.1, "recovery_mult": 0.9},
    WARNING: {"range": (50, 69), "sensitivity": 1.05, "recovery_mult": 1.0},
    NORMAL: {"range": (70, 89), "sensitivity": 1.0, "recovery_mult": 1.0},
    SAFE: {"range": (90, 100), "sensitivity": 0.95, "recovery_mult": 1.05},
}
```

### 9.2 自律等级参数

```python
DISCIPLINE_PARAMS = {
    HIGH: {
        "violation_penalty_range": (0.5, 1.5),   # 统一扣分范围
        "natural_recovery_range": (0.3, 0.6),    # 极低恢复，净恢复≈0
        "cumulative_factor": 0.005,
        "noise_sigma": 0.15,
        "resilience": 2.5,
        "plateau_duration": 14,
    },
    MEDIUM: {
        "violation_penalty_range": (0.5, 1.5),   # 统一扣分范围
        "natural_recovery_range": (1.0, 2.0),    # 中等恢复
        "cumulative_factor": 0.005,
        "noise_sigma": 0.3,
        "resilience": 1.4,
        "plateau_duration": 7,
    },
    LOW: {
        "violation_penalty_range": (0.5, 1.5),   # 统一扣分范围
        "natural_recovery_range": (1.5, 2.5),    # 中等恢复
        "cumulative_factor": 0.005,
        "noise_sigma": 0.4,
        "resilience": 1.2,
        "plateau_duration": 3,
    },
}
```

### 9.3 系统常量

```python
WARNING_LINE = 30           # 警戒线
SOFT_FLOOR = 20             # 软地板（分数低于20时限制下降）
LOW_DURATION_14 = 14        # 低分临界天数（干预必成功）
IMPROVEMENT_THRESHOLD = 10   # 自律提升所需连续无违规天数
MAX_DAILY_CHANGE = 10.0      # 每日最大变动（扣分或加分）
MAX_NOISE = 1.2              # 最大波动幅度
```

### 9.4 管理强度参数（动态）

```python
MANAGEMENT_PARAMS = {
    # 干预成功率
    "success_rate_normal": 0.60,
    "success_rate_1_2_days": 0.65,
    "success_rate_3_6_days": 0.75,
    "success_rate_7_13_days": 0.85,
    "success_rate_14plus_days": 1.0,  # 必成功

    # 遵从奖励
    "compliance_bonus_normal": 1.5,
    "compliance_bonus_1_2_days": 2.0,
    "compliance_bonus_3_6_days": 2.5,
    "compliance_bonus_7_13_days": 3.0,
    "compliance_bonus_14plus_days": 3.5,

    # 其他
    "intervention_delay_after_l3": 6,  # L3后延迟时间（小时）
}
```
