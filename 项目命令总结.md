# 项目命令总结（端到端流程）

本文汇总仓库内全部 Markdown 文档的运行命令，并按当前代码实现校准为可执行版本。

## 1. 目标与范围

- 覆盖从运行模拟到输出图表的完整链路：
  - 运行模拟
  - 压缩数据
  - 地图回放
  - 生成图表（`generate_charts.py`）
- 同时给出健康版与原始小镇版命令。

## 2. 一次性环境准备

在项目根目录执行：

```powershell
conda create -n generative_agents_cn python=3.12
conda activate generative_agents_cn
pip install -r requirements.txt
```

进入主代码目录：

```powershell
cd generative_agents
```

提示：
- 需要可用的 LLM 与 embedding 服务（如 Ollama），并在 `data/config.json` / `data/config_health.json` 中配置好模型与地址。

## 3. 健康版完整流程（推荐）

### 3.1 运行健康模拟（可视化版，推荐用于地图回放）

```powershell
python start_health_simulation_visual.py --scenario weight-loss --days 45 --batch --initial-health 75 --discipline medium
```

常用参数：
- `--scenario`：`weight-loss` / `phone-addiction` / `diabetes` / `home-diabetes` / `home-phone-addiction`
- `--days`：模拟天数
- `--resume`：断点续跑
- `--batch`：批处理模式（更快）
- `--initial-health`：`60|75|90`
- `--discipline`：`low|medium|high`

输出目录（按代码实际）：
- `results/health/{scenario}/{run_name}/`

### 3.2 压缩为回放数据

先查看可用运行：

```powershell
python compress_health.py --list
```

压缩最新一次运行：

```powershell
python compress_health.py --scenario weight-loss
```

或压缩指定运行：

```powershell
python compress_health.py --scenario weight-loss --run init75_medium_20260320_120241
```

输出目录：
- `results/compressed/health-{scenario}-{run_name}/movement.json`

### 3.3 地图回放

启动回放服务：

```powershell
python replay_health.py
```

浏览器打开：

```text
http://127.0.0.1:5001/
```

或直接打开指定运行：

```text
http://127.0.0.1:5001/?name=health-weight-loss-init75_medium_20260320_120241
```

### 3.4 数据分析与统计（analyze_health.py）

提取模拟结果中的关键指标并导出为 CSV 格式，便于进一步数据分析和对比。

**列出所有可用的场景和运行：**

```powershell
python analyze_health.py --list
```

**分析特定场景的最新运行：**

```powershell
python analyze_health.py --scenario weight-loss
```

**分析多个场景：**

```powershell
python analyze_health.py --scenario diabetes weight-loss phone-addiction
```

**分析特定的运行：**

```powershell
python analyze_health.py --scenario weight-loss --run init75_medium_20260320_120241
```

**指定输入/输出目录：**

```powershell
python analyze_health.py --scenario weight-loss --results-dir results/health --output-dir results/csv
```

**输出说明：**

- **日详细指标 CSV**：`health_analysis_{scenario}_{run}_{timestamp}.csv`
  - 包含每一天的 40+ 指标（健康分、满意度、干预次数、信任资本等）
  - 用于绘制曲线图、趋势分析、对比不同干预策略

- **综合摘要报告**：`health_analysis_summary_{timestamp}.csv`
  - 对所有分析的运行进行汇总
  - 包含初始/最终健康分、平均干预级别、总违规次数等聚合指标

**支持的指标覆盖范围：**
- 基本信息：日期、天数
- 健康指标：健康分、健康变化、健康分10分制
- 情绪指标：情绪分、满意度、心情状态
- 干预指标：干预次数、成功率、干预类型、平均强度
- 行为指标：违规次数、不当行为、零食次数、手机使用时长
- 策略指标：策略级别、策略阶段、习惯阶段、信任资本、声誉分数
- 派生指标：综合评分、效率分数、依从分数等

## 4. 生成图表（generate_charts.py）

### 4.1 关键前提

`generate_charts.py` 读取的是：

- `results/health/exp_1_init90_high.log` ... `exp_9_init60_low.log`

并输出到：

- `results/figures/01_all_experiments_overview.png` ... `08_summary_statistics.png`
- `results/figures/experiment_report.txt`

因此，先要产出上述 `exp_*.log` 文件，再执行图表脚本。

### 4.2 Windows PowerShell 生成 `exp_*.log`（推荐）

在 `generative_agents` 目录执行：

```powershell
$scenario = "weight-loss"
$days = 90
$experiments = @(
  @{n=1; h=90; d="high"},
  @{n=2; h=90; d="medium"},
  @{n=3; h=90; d="low"},
  @{n=4; h=75; d="high"},
  @{n=5; h=75; d="medium"},
  @{n=6; h=75; d="low"},
  @{n=7; h=60; d="high"},
  @{n=8; h=60; d="medium"},
  @{n=9; h=60; d="low"}
)

New-Item -ItemType Directory -Force -Path results/health | Out-Null

foreach ($e in $experiments) {
  $log = "results/health/exp_$($e.n)_init$($e.h)_$($e.d).log"
  python start_health_simulation.py --scenario $scenario --initial-health $($e.h) --discipline $($e.d) --days $days --batch *> $log
}
```

### 4.3 生成图表

```powershell
python generate_charts.py
```

英文版图表：

```powershell
python generate_charts_en.py
```

## 5. 原始小镇版流程（非健康版）

### 5.1 运行模拟

```powershell
python start.py --name sim-test --start "20250213-09:30" --step 10 --stride 10
```

断点续跑：

```powershell
python start.py --name sim-test --resume --step 10 --stride 10
```

### 5.2 压缩

```powershell
python compress.py --name sim-test
```

### 5.3 回放

```powershell
python replay.py
```

浏览器打开：

```text
http://127.0.0.1:5000/?name=sim-test&step=0&speed=2&zoom=0.8
```

## 6. 易错点与统一口径

- `replay_health.py` 当前代码端口是 `5001`。
- `compress_health.py` 使用 `--scenario` / `--run` / `--list`，不是 `--name`。
- `start_health_simulation.py` 主参数是 `--scenario`，支持 `--run-all`，但 `generate_charts.py` 仍依赖 `results/health/exp_*.log`。
- 如果没有图表输出，先检查：
  - 是否存在 `results/health/exp_*.log`
  - `matplotlib` 是否可用

## 7. 最小可运行命令链（健康版）

```powershell
conda activate generative_agents_cn
cd generative_agents
python start_health_simulation_visual.py --scenario weight-loss --days 7 --batch
python compress_health.py --scenario weight-loss
python replay_health.py
```

再开一个终端生成图表：

```powershell
cd generative_agents
python generate_charts.py
```
