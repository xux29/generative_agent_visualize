根据活动意图，确定最合适的语义位置。

${base_desc}

当前活动意图：${intent}

可用位置及其描述：
${locations_desc}

分析要求：
1. 理解活动意图的核心需求（如：需要食物、需要休息、需要使用设备等）
2. 评估每个位置是否能满足这个活动需求
3. 选择最匹配的位置名称

必须从以下位置中选择一个：[${location_keys}]
如果活动不需要特定位置，选择 "bedroom" 作为默认。

选中的位置名称：
